<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inovel.mapper.ChapterMapper">

    <resultMap id="ChapterResultMap" type="com.inovel.entity.Chapter">
        <id property="id" column="id"/>
        <result property="workId" column="work_id"/>
        <result property="chapterNum" column="chapter_num"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="wordCount" column="word_count"/>
        <result property="viewCount" column="view_count"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectById" resultMap="ChapterResultMap">
        SELECT * FROM chapters WHERE id = #{id}
    </select>

    <select id="selectByWorkId" resultMap="ChapterResultMap">
        SELECT * FROM chapters WHERE work_id = #{workId} ORDER BY chapter_num ASC
    </select>

    <select id="selectByWorkIdAndChapterNum" resultMap="ChapterResultMap">
        SELECT * FROM chapters WHERE work_id = #{workId} AND chapter_num = #{chapterNum}
    </select>

    <insert id="insert" parameterType="com.inovel.entity.Chapter" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chapters (work_id, chapter_num, title, content, word_count, view_count, 
                            create_time, update_time)
        VALUES (#{workId}, #{chapterNum}, #{title}, #{content}, #{wordCount}, #{viewCount},
                #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        UPDATE chapters
        SET title = #{title},
            content = #{content},
            word_count = #{wordCount},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM chapters WHERE id = #{id}
    </delete>

    <delete id="deleteByWorkId">
        DELETE FROM chapters WHERE work_id = #{workId}
    </delete>

    <update id="updateViewCount">
        UPDATE chapters SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <select id="countByWorkId" resultType="int">
        SELECT COUNT(*) FROM chapters WHERE work_id = #{workId}
    </select>

    <select id="sumWordCountByWorkId" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(word_count), 0) FROM chapters WHERE work_id = #{workId}
    </select>

</mapper>
